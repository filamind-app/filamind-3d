# Optional overrides. Without these the app derives Moonraker from the current host.
# VITE_MOONRAKER_WS_URL=ws://printer.local:7125/websocket
# VITE_APP_TITLE=FilaMind 3d

# Dev-server proxy target for /server, /printer, /websocket, … (vite.config.ts):
# MOONRAKER_HTTP=http://printer.local:7125
